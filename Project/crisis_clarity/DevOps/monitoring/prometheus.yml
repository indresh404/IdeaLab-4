global:
  scrape_interval: 15s # How often to scrape targets

scrape_configs:
  - job_name: 'crisisclarity-backend'
    metrics_path: '/metrics'
    scrape_interval: 5s
    static_configs:
      - targets: ['backend:8000']
